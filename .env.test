# Test Environment Configuration
# This file contains environment variables specifically for E2E testing

# Relax rate limiting for testing (allows more frequent requests)
RELAX_RATE_LIMITS=true

# Delay between test requests to avoid rate limiting (milliseconds)
TEST_DELAY_MS=1500

# Maximum retries for rate-limited requests
TEST_MAX_RETRIES=3

# Request timeout for individual test requests (milliseconds)
TEST_TIMEOUT_MS=15000

# API Base URL (override if needed)
# API_BASE_URL=https://mcm5t8vma2.execute-api.us-east-1.amazonaws.com'

# Test user settings
TEST_USER_PREFIX=e2etest

USER_POOL_ID=us-east-1_R913UZfFp
USER_POOL_CLIENT_ID=ttgerktvrcv7jmj1gn3dcjgn1
AWS_REGION=us-east-1 # Or your specific region if different
